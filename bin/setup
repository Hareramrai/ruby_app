# Add script to:
# * Install dependencies
# * Build/Compile
# * Run Test Suit to validate
#
# After this is run, bin/parser
# should Just Work.
#!/usr/bin/env ruby
gem install bundler -v '1.17.2'
bundle install
